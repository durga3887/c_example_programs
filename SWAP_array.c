#include <stdio.h>

void swap(int *a, int *b)
{
    int count = 0; 
    while((*(a+count) !='\0') || (*(b+count) != '\0'))
    {
        if(*(a+count) != '\0' )
        {
            if(*(b+count) != '\0' )
            {
                *(a\+count) = *(a+count) + *(b+count);
                *(b+count) = *(a+count) - *(b+count);
                *(a+count) = *(a+count) - *(b+count);
                
            }
            else
            {
                
            }
        }
        if(*(b+count) != '\0' )
        {
            
        }
    }
}

int main()
{
    return *address;

    return 0;
}
